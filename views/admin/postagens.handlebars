<h2>Lista de Postagens:</h2>
<hr>
<a href="/admin/postagens/add"<button class="btn btn-success">Nova Postagem</button></a>


{{#each postagens}}

<div class="card mt-4">
<div class="card-body">
    <h4>{{titulo}}</h4>
    <small>{{slug}}</small><br>
    <small>{{descricao}}</small><br>
    <small>{{conteudo}}</small><br>
    <small>{{categoria}}</small><br>
    <small>{{data}}</small><br>
    <a href="/admin/postagens/edit/{{_id}}"><button class="btn btn-primary mt-4">Editar Postagem</button></a>
    <form action="/admin/postagens/deletar" method="POST">
        <input type="hidden" name="id" value="{{_id}}">
        <button class="btn btn-danger mt-4">Deletar Postagem</button></a>
    </form>
</div>
</div>

{{else}}
<h4 class="mt-3">Postagens vazias.</h4>
{{/each}}